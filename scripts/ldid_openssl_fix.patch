diff --git a/ldid.cpp b/ldid.cpp
index 7e6f5d4..8a9b0c1 100644
--- a/ldid.cpp
+++ b/ldid.cpp
@@ -6,6 +6,7 @@
 #include <fstream>
 #include <iostream>
 #include <memory>   // 新增：引入 unique_ptr
+#include <vector>  // 新增：引入 vector 替代 VLA
 #include <set>
 #include <sstream>
 #include <string>
@@ -564,9 +565,9 @@ static inline void put(std::streambuf &stream, const void *data, size_t size, cons
 }
 
 static inline void pad(std::streambuf &stream, size_t size) {
-    char padding[size]; // 非标准 C++ VLA
-    memset(padding, 0, size);
-    put(stream, padding, size);
+    std::vector<char> padding(size, 0); // 标准 C++ 动态数组
+    put(stream, padding.data(), size);
 }
 
 template <typename Type_>
@@ -2031,8 +2032,9 @@ static void get(std::string &value, X509_NAME *name, int nid) {
         fprintf(stderr, "ldid: An error occured while parsing the certificate: %s\n", ERR_error_string(ERR_get_error(), NULL));
         exit(1);
     }
-    value.assign(reinterpret_cast<char *>(ASN1_STRING_data(asn)), ASN1_STRING_length(asn));
+    // OpenSSL 3.x 使用 get0_data 获取只读指针
+    const unsigned char *data_ptr = ASN1_STRING_get0_data(asn);
+    value.assign(reinterpret_cast<const char *>(data_ptr), ASN1_STRING_length(asn));
 }
 
 static void req(std::streambuf &buffer, uint32_t value) {
@@ -2728,7 +2730,7 @@ struct Rule {
     unsigned weight_;
     Mode mode_;
     std::string code_;
-    mutable std::auto_ptr<Expression> regex_; // 已废弃
+    mutable std::unique_ptr<Expression> regex_; // C++11+ 标准智能指针
 
     Rule(unsigned weight, Mode mode, const std::string &code) :
         weight_(weight),
@@ -2738,7 +2740,7 @@ struct Rule {
     }
 
     void Compile() const {
-        regex_.reset(new Expression(code_)); // auto_ptr 使用 reset
+        regex_.reset(new Expression(code_)); // unique_ptr 同样使用 reset
     }
 
     bool operator ()(const std::string &data) const {
@@ -3532,9 +3534,9 @@ static Hash Sign(const uint8_t *prefix, size_t size, std::streambuf &buffer, Has
                         algorithm(hash, blob + begin, end - begin);
                         break;
                     case 3: // SHA256
-                        std::vector<uint8_t> hash(algorithm.size_);
+                        std::vector<uint8_t> hash_vec(algorithm.size_); // 改名避免冲突
                         algorithm(
-                            hash.data(),
+                            hash_vec.data(), // 使用 vector 的数据指针
                             blob + begin,
                             end - begin
                         );
-                        Hex(hash, 20); // 这里需要调整，假设 Hex 接受 vector
+                        Hex(hash_vec.data(), hash_vec.size()); // 显式传递数据和大小
                         break;
                 }
                 break;
